---
doctype: dist

header:
  name: CairoX-Sweet

plugins:
  - +config: Author::CSSON

  - +extend_plugin: Git::GatherDir
    exclude_match: \.png

  - +add_plugin: TravisYML
    +before: Git::Tag
    perl_version: 5.16
    post_before_install: sudo apt-get install libcairo2-dev
    pre_install: cpanm Cairo

  - +plugin: MetaProvides::FromFile
    file: meta.ini

---
doctype: weaver

plugins:
  - +config: Author::CSSON

  - +remove_plugin: Splint

#
#
#[@Author::CSSON]
#is_private = 0
#has_travis = 1
#Git::GatherDir.exclude_match = \.png
#
#TravisYML.perl_version = 5.16
#TravisYML.post_before_install = sudo apt-get install libcairo2-dev
#TravisYML.pre_install = cpanm Cairo
#
#NextVersion::Semantic.major =
#NextVersion::Semantic.minor = API Changes, New Features, Enhancements
#NextVersion::Semantic.revision = Revision, Bug Fixes, Documentation, Meta
#NextVersion::Semantic.format = %d.%02d%02d
#NextVersion::Semantic.numify_version = 0
#
#[MetaProvides::FromFile]
#file = meta.ini
